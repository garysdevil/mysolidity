## 编译
```bash
npm install @openzeppelin/contracts
npm install @truffle/hdwallet-provider

npm install ini
npm install web3
npm install merkletreejs
npm install keccak256
```

```bash
truffile compile
truffle migrate --network huygens
truffle deploy --reset --network development
```